<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <%= link_to 'Главная', root_path, class: 'navbar-brand' %>
        <%= link_to 'Касса', new_receipt_path, class: 'navbar-brand' if current_user.has_role?(:receipts,:new)  %>
        <%= link_to 'Продукты', items_path, class: 'navbar-brand' if current_user.has_role?(:items,:edit) %>
        <%= link_to 'Склад', batches_path, class: 'navbar-brand'  if current_user.has_role?(:batches,:index) %>
        <%= link_to 'Чеки', receipts_path, class: 'navbar-brand' if current_user.has_role?(:receipts,:index)  %>
        <%= link_to 'Отчеты', reports_path, class: 'navbar-brand' if current_user.has_role?(:reports,:index)%>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <%= render 'layouts/nav_links_for_auth' %>
      </ul>
    </div>
  </div>
</nav>

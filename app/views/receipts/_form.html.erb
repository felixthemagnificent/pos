<%= simple_form_for(@receipt) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :total %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
